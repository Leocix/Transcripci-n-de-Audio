# Minimal dependencies for running the FastAPI app (recommended for production base image)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1
requests>=2.31.0

# Lightweight audio handling (keep minimal; add full stack in optional requirements)
pydub>=0.25.1
soundfile>=0.12.1

# Document export (optional: include if you need DOCX/PDF exports)
# python-docx>=0.8.12
# reportlab>=4.0.0

# For heavy ML (whisper/torch/pyannote) install `config/requirements-optional.txt`

# Optional / heavy dependencies (install these only on machines with GPUs or enough RAM)
# Examples: for diarization and high-accuracy transcription

# Core ML and audio models
openai-whisper==20250625
torch==2.8.0
torchaudio==2.8.0

# Pyannote (diarization)
pyannote.audio==4.0.1
pyannote-core==6.0.1
pyannote-database==6.1.0
pyannote-metrics==4.0.0
pyannote-pipeline==4.0.0
pyannoteai-sdk==0.3.0

# ML / acceleration (vendor-specific CUDA wheels are recommended)
torchmetrics==1.8.2
triton==3.4.0

# Heavy audio & analysis libraries
librosa==0.11.0
av==15.1.0
soxr==1.0.0
soundfile==0.13.1

# Optional tooling
python-docx==1.2.0
reportlab==4.4.4

# Hugging Face utilities
huggingface-hub==0.35.3

# Other heavy packages captured in install logs (pin versions to reduce surprises)
numpy==2.3.3
scipy==1.16.2
pandas==2.3.3
matplotlib==3.10.7

# Utilities
# Use a permissive requirement for boto3 to avoid CI build failures when a specific
# micro version isn't available in the build environment. CI will install the
# latest compatible boto3 (>=1.26). Pin further if you need a strict version.
boto3>=1.26.0

# Note: For GPU support, prefer installing vendor-specific CUDA wheels and following
# the respective library installation instructions (torch, torchaudio, triton, etc.).

