
--extra-index-url https://download.pytorch.org/whl/cpu

# Optional / heavy dependencies for CPU-only environments
# This file forces CPU-only PyTorch wheels to avoid downloading large CUDA/NVIDIA packages

# Core ML and audio models (CPU wheels)
openai-whisper==20250625
torch==2.8.0+cpu
torchaudio==2.8.0+cpu

# Pyannote (diarization)
pyannote.audio==4.0.1
pyannote-core==6.0.1
pyannote-database==6.1.0
pyannote-metrics==4.0.0
pyannote-pipeline==4.0.0
pyannoteai-sdk==0.3.0

# ML / acceleration (CPU-friendly where possible)
torchmetrics==1.8.2
triton==3.4.0

# Heavy audio & analysis libraries
librosa==0.11.0
av==15.1.0
soxr==1.0.0
soundfile==0.13.1

# Optional tooling
python-docx==1.2.0
reportlab==4.4.4

# Hugging Face utilities
huggingface-hub==0.35.3

# Other heavy packages
numpy==2.3.3
scipy==1.16.2
pandas==2.3.3
matplotlib==3.10.7

# Utilities
boto3>=1.26.0

# NOTE: When building inside Docker, instruct pip to use the PyTorch CPU index:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r config/requirements-full-cpu.txt

